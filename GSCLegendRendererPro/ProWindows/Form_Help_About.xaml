<controls:ProWindow x:Class="GSCLegendRendererPro.ProWindows.Form_Help_About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:GSCLegendRendererPro.Converters"
        xmlns:local="clr-namespace:GSCLegendRendererPro.Properties"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="{x:Static local:Resources.FormHelpTitle}" MaxHeight="350" MaxWidth="285"
        WindowStartupLocation="CenterOwner">
    <controls:ProWindow.Resources>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
                <extensions:DesignOnlyResourceDictionary Source="/GSCLegendRendererPro;component/LegendUIStyle.xaml"/>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component/XamlImages.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <conv:ColorFromBoolConverter x:Key="bool2Color"/>
        </ResourceDictionary>

    </controls:ProWindow.Resources>
        
    <Grid Style="{DynamicResource LegendGrid}" Margin="10" VerticalAlignment="Top">

        <Grid.RowDefinitions>

            <!--Addin infomation-->
            <RowDefinition Height="100"/>

            <!--Help-->
            <RowDefinition Height="100"/>

            <!--More-->
            <RowDefinition Height="120"/>

        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" >
            <Label Content="{x:Static local:Resources.FormHelpFirstSection}" FontWeight="Bold"/>
            <Label Margin="5,5,5,0" Content="{Binding AddinVersion}" />
            <Label Margin="5,0,5,5" Content="{Binding AddinVersionLatestOnline}" Foreground="{Binding IsValid, Converter={StaticResource bool2Color}}" />
        </StackPanel>

        <StackPanel Grid.Row="1" >
            <Label Content="{x:Static local:Resources.FormHelpSecondSection}" FontWeight="Bold"/>
            <Button x:Name="ButtonHelpProject" Command="{Binding OpenOnlineProject}" Content="{x:Static local:Resources.FormHelpOpenProject}" Style="{DynamicResource Esri_Button}"/>
            <Button x:Name="ButtonHelpIssue" Command="{Binding OpenOnlineIssue}" Content="{x:Static local:Resources.FormHelpOpenIssue}" Style="{DynamicResource Esri_Button}"/>

        </StackPanel>

        <StackPanel Grid.Row="2">
            <Label Content="{x:Static local:Resources.FormHelpThirdSection}" FontWeight="Bold"/>
            <TextBlock Text="{x:Static local:Resources.FormHelpPrimaryContact}" TextWrapping="Wrap" Margin="10,5,5,5">
                <Hyperlink NavigateUri="mailto:gabriel.huot-vezina@NRCan-RNCan.gc.ca?subject=GSC%20Legend%20Renderer%20Add-in%20Inquiry" RequestNavigate="Hyperlink_RequestNavigate">Gabriel Huot-Vézina</Hyperlink>
            </TextBlock>
            <TextBlock Text="{x:Static local:Resources.FormHelpSecondaryContact}" TextWrapping="Wrap" Margin="10,5,5,5">
                <Hyperlink NavigateUri="mailto:katarzyna.zawadzka@NRCan-RNCan.gc.ca?subject=GSC%20Legend%20Renderer%20Add-in%20Inquiry" RequestNavigate="Hyperlink_RequestNavigate">Katarzyna Zawadzka</Hyperlink>
            </TextBlock>
        </StackPanel>

    </Grid>
        
</controls:ProWindow>
